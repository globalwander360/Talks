@Repository
public interface ExternalTrackAndTraceRepository extends JpaRepository<ExternalTrackAndTrace, Long> {

    @Query("SELECT e.prodCode FROM ExternalTrackAndTrace e WHERE e.eventEntity.id = :eventId")
    String findProdCodeByEventId(@Param("eventId") Long eventId);
}
